import Article from "./Article";
import NewsList from "./NewsList";
async function page() {
  // const news = await FetchNews(categories.join(","));
  return (
    <div>
      <Article/>
    </div>
  );
}

export default page;
